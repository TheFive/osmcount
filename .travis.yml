 language: node_js
 
 node_js:
   - "0.10"

install:
  - "npm install"

services:
  - mongodb

before_script:
  - "sleep 15"
  - "mongo test --eval 'db.addUser("test", "test");'"

script:
  - "npm test"
